# Core
Django>=4.2,<5.0

# Production server (para despliegue)
gunicorn>=21.2.0

# Manejo de variables de entorno
python-decouple>=3.8

# Servir archivos estáticos en producción
whitenoise>=6.6.0

# Base de datos PostgreSQL (opcional, para producción)
psycopg2-binary>=2.9.9

# Seguridad
django-cors-headers>=4.3.1
